# 家計簿・日程表・資産管理アプリ 仕様書（v1）

## 1. 概要
家族で共有して使う、家計簿・日程表・資産管理（貯蓄/投資含む）を統合したWebアプリ。

## 2. 技術構成
- フロントエンド / バックエンド: Next.js (App Router, TypeScript)
- DB: スプレッドシートで別途作成・後日連携（v1ではダミーデータで画面のみ実装）
- UI: Tailwind CSS、グラフは recharts
- ホスティング: 未定（後日決定）
- 認証: 世帯共通の1パスワードによる簡易ログイン（v1では未実装、画面のみ想定）

## 3. 利用形態
- 家族など複数人で共有して利用
- ログインは世帯共通の1パスワード（個別アカウントは作らない）
- アプリ内に「メンバー」概念を持たせ、名前・色を登録（ログインとは別）
- 家計簿の支出・日程表の予定は、それぞれ「どのメンバーのものか」を紐付け可能

## 4. 機能仕様

### 4.1 ダッシュボード（ホーム画面）
- ログイン後の最初のページ（トップ画面）
- 資産総額を「ヒーロー数字」として最上部に大きく表示し、先月比の増減（▲/▼%）を併記
- 今月の収支サマリー（収入・支出・差引の3カード）
- 直近の予定一覧（タップで日程詳細画面へ）
- 画面下部のタブバーから各画面へ遷移

### 4.2 家計簿
- 収入・支出の手動記録（日付・金額・カテゴリ・メモ・記録者（メンバー））
- カテゴリ分類（食費・光熱費・住居費など初期カテゴリ＋カスタム追加）
- 月次・年次の切り替え表示、集計とグラフ表示（カテゴリ別内訳は円グラフ＋横棒グラフの併用、月別/年別推移）
- 一覧のCRUD（登録・編集・削除）
- 予算アラート・定期収支の自動登録・レシート画像添付は **対象外**（将来拡張候補、9章参照）

### 4.3 日程表
- 予定のCRUD（登録・編集・削除）
- 月表示・週表示のカレンダーUI
- メンバーごとの色分け表示・フィルター
- 繰り返し予定（毎週・毎月など）に対応
- 予定をタップすると横スライドの詳細画面に遷移し、詳細画面から編集・削除を行う（一覧から直接編集モーダルへは遷移しない）
- 家計簿との連携（支払い予定日のリマインド表示）は将来拡張候補

### 4.4 資産管理
- 記録対象: 現金・銀行預金（普通/定期）、投資信託・株式・ETF
- 資産ごとに「資産口座（AssetAccount）」を登録し、任意のタイミングで残高・評価額のスナップショットを何度でも記録
- 資産口座をタップすると横スライドの詳細画面に遷移し、その口座単体の推移グラフ・記録履歴・残高記録の追加/編集/削除を行う
- 資産推移グラフ（時系列、全口座合計）
- 資産配分（アセットアロケーション）の円グラフ
- 株価等の自動取得・外部API連携は **v1では対象外**（手動入力のみ）

### 4.5 横断的なUI仕様（2026-08-19 更新: Copilot Money 風デザインへ刷新）
- 配色: ニュートラルな白／黒基調の背景＋グリーンのブランドアクセント（旧: クリーム＋アンバー）。収入＝緑／支出＝赤の財務的な色分けはブランドカラーと独立して維持
- カード影は暖色タイントを廃し、ニュートラルな `--shadow-card` / `--shadow-card-lg` トークンに統一
- カテゴリ・メンバー・資産口座の一覧行は、単色ドットではなく色付き円形アバター（`ColorAvatar`、頭文字入り）で表示
- ダッシュボードの資産総額カードに直近6ヶ月の推移スパークラインを追加
- 資産推移グラフはグラデーション塗りつぶし付きのエリアチャートに変更
- レイアウト: `md`（768px）以上の画面幅ではデスクトップ向け左サイドバーナビゲーションを表示し、ボトムタブバーは非表示にする（Copilot Moneyのデスクトップ/タブレット体験を参考）。`md` 未満は従来通りボトムタブバー
- ダークモード: OSの配色設定（`prefers-color-scheme`）に自動追従。手動切り替えは無し
- 保存・削除などの操作結果はトースト通知でフィードバック
- 一覧が空の場合はアイコン付きの空状態表示（「記録はありません」等の素のテキストのみの表示はしない）
- ナビゲーションはボトムタブバー（角丸・半透明のガラス風デザイン、モバイル）／左サイドバー（デスクトップ）

## 5. データモデル（ドラフト・スプレッドシート側で実装予定）

```
Member        id, name, color
Category      id, name, type(income/expense), color
Transaction   id, date, amount, type(income/expense), categoryId, memberId, memo
Event         id, title, startAt, endAt, allDay, memberId, recurrenceRule, memo
AssetAccount  id, name, type(cash/bank/investment), memberId(nullable)
AssetSnapshot id, assetAccountId, date, value, note
```

## 6. 画面構成
- ホーム（ダッシュボード） … トップ画面
- 家計簿 … 一覧・グラフ・登録編集
- 日程表 … カレンダー（月・週表示）／予定詳細画面（`/schedule/[id]`、横スライド遷移）
- 資産管理 … 一覧・推移グラフ／資産口座詳細画面（`/assets/[id]`、横スライド遷移）
- 設定 … メンバー管理、カテゴリ管理、資産口座管理

ナビゲーションは画面下部のタブバー（ホーム／家計簿／日程表／資産管理／設定）から各画面へ遷移する構成。スマートフォンでの片手操作を優先し、目的地が常に見えるタブバー方式を採用（ハンバーガーメニューから変更）。

## 7. v1スコープ外（将来拡張候補）
- 個別アカウント・権限管理（閲覧のみ等）
- 予算超過アラート
- 定期収支の自動登録
- レシート画像添付
- 株価・為替の自動取得連携
- 銀行・証券口座との自動連携（API連携）
- 家計簿と日程表の自動連携（支払いリマインド）

## 8. 開発方針（更新）
- DBはスプレッドシートで別途作成し、後日連携する
- v1では画面（UI）のみを、ダミーデータを使って実装する
- ナビゲーション: ホーム画面をトップとし、画面下部のタブバーから各画面へ遷移

## 9. 現状評価と改善候補（監査・2026-08-19時点）

「完璧なアプリか」という問いへの結論: **いいえ。** 見た目の作り込みとメンバー紐付け・年次集計などの機能は一通り揃ったが、エラー処理・誤操作対策・一部UXは詰め切れていない。優先度つきで課題を残す。

### 9.1 デザインで洗練できる点
- 削除確認がブラウザ標準の `confirm()` のまま。トースト通知やスライド画面など他の部分をカスタムUIで作り込んでいるのに対し、ここだけOS標準の無骨なダイアログが出るのは明らかに浮いている。カスタムの確認モーダルに置き換えるべき。
- フォームの入力チェックがブラウザ標準のバリデーション任せで、赤枠＋エラーメッセージのようなインライン検証がない。
- フォーカスリング（キーボード操作時に要素を囲む枠線）がブラウザ既定の青のままで、アンバーのブランドカラーに統一されていない。
- 空状態のアイコンは簡易的な線画のみで、画面ごとの作り分けはあるが温かみは薄い。
- Rechartsの軸ラベル色をCSS変数（`var(--muted)` 等）でダークモードに追随させているが、データがある状態での実機ダークモード表示は未確認（グラフにデータが乗った状態でのスクリーンショット確認が必要）。

### 9.2 UXで改善できる点
- 削除に「元に戻す」がない。`confirm()` 一発で削除されるため、誤操作時の救済策がない。
- 家計簿にメンバーフィルターがない（日程表にはあるのに）。「たろうの支出だけ見る」ができない。
- 検索が家計簿の中でしか使えない。予定・資産記録は横断検索できない。
- 通信エラー・GAS側の障害時に専用のエラー画面がなく、Next.jsの汎用エラー画面に落ちる。GAS APIが単一障害点になっており、落ちると家族全員が使えなくなる。
- ログイン試行回数の制限がない（合言葉は理論上総当たり可能）。世帯内利用が前提のため緊急度は低いが、記録として残す。

### 9.3 内部コードの品質面
- `addTransaction` 等のサーバーアクション呼び出し箇所に、成功/失敗を判定する `try/catch` が一切ない。楽観的更新（ローカルstateを先に更新する処理）のロールバックもないため、通信エラー時に「画面上は保存済みだが実際は保存されていない」という不整合が起きうる。**実利用上、最も影響が大きい課題。**
- レコードのID発行が `` `t${Date.now()}` `` 方式。複数端末からの同時操作では理論上IDが衝突しうる。
- 支出記録・予定登録・資産記録・メンバー登録など、似た構造のCRUDフォームが5箇所以上でほぼ重複している。共通化の余地がある。
- 自動テストが一切存在しない。今回のような大規模な変更もPlaywrightによる手動確認のみに頼っている。

### 9.4 優先度の提案
実際の家族利用への影響が大きい順:
1. サーバーアクションのエラーハンドリング（保存失敗に気づけないのが一番危険）
2. `confirm()` の置き換え（誤操作による削除の防止）
3. 家計簿のメンバーフィルター
4. 予定・資産記録を含む横断検索

## 10. レスポンシブ・折りたたみ仕様（2026-08-31時点）

`voltagent/awesome-design-md`（Revolut / Wise / Cal.com / Linear / Apple 等の実装済みUIをトークン化したリポジトリ）に倣い、「何px・どの条件で何が変わるか」を表として明文化しておく。今後AIエージェントが画面を変更する際、この表を更新することで挙動の記録を残す。

### 10.1 レイアウト全体の切り替え
| 名前 | 幅 | 変化 |
|---|---|---|
| Mobile | < 768px | ボトムタブバー（角丸・半透明ガラス）を表示、左サイドバーは非表示 |
| Desktop | ≥ 768px | 左サイドバーを表示、ボトムタブバーは非表示 |

### 10.2 折りたたみ（Collapsible）のデフォルト状態
スマホでの「画面が長くてスクロールが大変」という声を受けて導入。既定で開くか閉じるかは「その場に来た主目的に直結するか」で判断している。

| 画面 | 対象 | 既定 | 理由 |
|---|---|---|---|
| 家計簿 | カテゴリ別支出＋月別/年別推移（グラフを見る） | 閉 | 明細一覧を見に来ることが多く、グラフは補助情報のため |
| 資産管理 | 資産配分＋資産推移（グラフを見る） | 閉 | 口座一覧・最近の記録を優先表示するため |
| 設定 › メンバー/カテゴリ/資産口座 | 各「追加」フォーム | 一覧が空のときのみ開、それ以外は閉 | 追加が目的で来た可能性が高いときだけ自動的に開く |

グラフ内でデータが1件もない場合は、装飾アイコンだけでなく薄いダミーのグラフ形状（円グラフ/折れ線のシルエット）を出し、「記録すればここに何が表示されるか」を先取りして見せる。

### 10.3 タッチターゲット・数値表示の基準
- アイコンボタン（編集・削除等）: 44×44px（Apple/WCAGの基準）
- 金額入力欄: 高さ・文字サイズを他の入力欄より大きくし（22px前後）、フォーカスが外れている間は3桁区切り表示にする
- 資産総額・口座残高など主要な数字には `text-numeral` ユーティリティ（等幅数字＋負のletter-spacing）を使う
- カードの角丸は `rounded-2xl`（16px）を上限とし、最重要カード（資産総額のヒーローカード等）だけ `rounded-3xl`（24px）まで許容する

### 10.4 ブランドカラーの使用方針
ブランドグリーンは「実行アクション」（送信・追加ボタン、ボトムナビの現在地）に予約し、月次/年次・月/週のようなセグメントコントロールや、メンバー絞り込みチップの選択状態にはニュートラルな配色（`text-foreground` + `bg-surface`の浮き上がり）を使う。1画面に同時発光するブランド色の数を抑えることで、「押せる/実行できる」色の意味を保つ。

## 11. 日本語タイポグラフィ・カラートークン方針（2026-08-31時点）

[kzhrknt/awesome-design-md-jp](https://github.com/kzhrknt/awesome-design-md-jp)（日本のサービスのDESIGN.mdを集めたリポジトリ。和文タイポグラフィの仕様を明文化している点が[本家awesome-design-md](https://github.com/VoltAgent/awesome-design-md)との違い）と、家計簿という同じ領域のマネーフォワード、フィンテックのスマートバンク（B/43）のDESIGN.mdを読み、household-appに合う部分だけを取り込んだ。

### 11.1 フォント・組版
- **フォントスタックは和文優先**: `Noto Sans JP`（next/font経由）→ OS標準の和文ゴシック → 欧文 → generic の順。以前は欧文フォント（SF Pro Text）が先頭にあり、和欧混植でウェイト・x-heightがちぐはぐになっていた
- **本文の行間は1.7**（欧文基準の1.4〜1.5だと和文は詰まって見える）
- **本文の字間は0.03em**（句読点まわりの余白を確保。数字など詰めたい要素は個別に上書きする）
- **`font-feature-settings: "palt"` を本文に適用**（句読点・かっこのアキを詰めるプロポーショナル組版）。ただし入力欄（input/select/textarea）は桁を数えやすい等幅のまま除外する
- 数字専用の `.text-numeral`（tabular-nums + 負のletter-spacing）は本文の字間ルールより優先され、共存する

### 11.2 文字色はアルファ付きの黒/白に統一
`--foreground` / `--subtle` / `--muted` は固定hexのグレーではなく、`rgba(0,0,0,0.88/0.62/0.4)`（ダークモードは白ベースで`0.92/0.72/0.52`）というアルファ表現にした（スマートバンクのText Default High/Mid/Lowに倣った）。数値は元のhexとほぼ同じ見え方になるよう逆算してあるので、既存画面のコントラストは変えていない。狙いは、資産総額カードのような色付き背景の上に文字を置いたときでも、固定hexのように「その面だけ浮いて見える」ことがなくなる点。

### 11.3 状態表現（state layer）
押下時のフィードバックは、要素ごとに`active:bg-track`や`active:opacity-70`をバラバラに書くのではなく、`.state-layer`ユーティリティ（`--state-overlay`のニュートラルな膜を重ねる）に寄せていく。現状は`IconButton`の編集ボタンと`MemberFilterChips`に適用済みで、他のインタラクティブ要素は今後同じパターンに揃えていく余地がある。危険操作（削除など）は例外として、あえて意味のある色（rose系）で押下フィードバックを出す。

### 11.4 検討したが採用しなかった案
- **マネーフォワード式の業務ツール的な密度・角丸4px**: 情報密度は高いが、household-appは「家族で使う」プロダクトで、現状の丸みのあるコンシューマー向けトーンの方が合っている。角丸は10.3節のルールを維持
- **調和数列（128÷n）によるタイポスケールの全面刷新**: 原則としては魅力的だが、既存の11〜44pxの実測値を一括で置き換えるのは影響範囲が大きすぎるため見送り。新規に文字サイズを追加するときの指針として覚えておく
- **フォーカスリングをアルファ黒に変更**: 既にブランドグリーンで統一済み（9章参照）のため変更なし
